# The real tenant host is configuration, not source — see odc-tenant.example.
odc-tenant

.DS_Store
.claude/settings.local.json
reference/raw/*.prev

# Finder/iCloud duplicate copies ("foo 2.css") are never source.
* [0-9].*

# The deployed app URL is configuration, not source — see odc-app.example.
odc-app

# Playwright is only needed for tools/test_browser.js.
node_modules/
